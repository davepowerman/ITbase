<% provide(:title, @user.name) %>

<div class="row">
	<aside class="span4">
		<section class="user">
			<h3>
				<%= gravatar_for @user %>
				<%= @user.name %>
			</h3>
		</section>
	</aside>
</div>
